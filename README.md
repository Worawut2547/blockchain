# 🔗 Blockchain Project

โปรเจกต์ระบบ Blockchain พื้นฐานที่พัฒนาด้วย Python รองรับการสร้างบล็อก การขุด (Mining) ด้วย Proof of Work และการตรวจสอบความถูกต้องของ Chain

---

## 📁 โครงสร้างโปรเจกต์

```
blockchain/
├── core/                  # Core blockchain logic
│   ├── __init__.py
│   └── blockchain.py      # Block, Blockchain, และ Mining classes
├── cmd/
│   ├── local/            # Local testing
│   │   ├── __init__.py
│   │   └── main.py       # ทดสอบ blockchain แบบ local
│   └── serve/            # Web API server
│       ├── __init__.py
│       └── app.py        # Flask REST API
└── README.md
```

---

## 🧩 ขั้นตอนวิธีสำคัญ (Algorithm)

### 1️⃣ **Block Structure**
แต่ละ Block ประกอบด้วย:
- `index`: ลำดับของบล็อกใน chain
- `timestamp`: เวลาที่สร้างบล็อก
- `body`: ข้อมูลธุรกรรมหรือข้อมูลที่เก็บในบล็อก
- `previous_hash`: Hash ของบล็อกก่อนหน้า
- `nonce`: ตัวเลขที่ใช้ในการขุด (Proof of Work)
- `current_hash`: Hash ของบล็อกปัจจุบัน (SHA-256)

### 2️⃣ **Proof of Work (PoW)**
การขุดบล็อกใหม่ใช้อัลกอริทึม Proof of Work:


hash = SHA256(equation)
ต้องหา nonce ที่ทำให้ hash ขึ้นต้นด้วย '0000'
```

> **หมายเหตุ:** ยิ่งจำนวน 0 มาก ความยากในการขุดก็จะยิ่งสูงขึ้น

### 3️⃣ **Chain Validation**
ตรวจสอบความถูกต้องของ Blockchain โดย:
✅ ตรวจสอบว่า `previous_hash` ของแต่ละบล็อกตรงกับ `current_hash` ของบล็อกก่อนหน้า  
✅ ตรวจสอบว่า Proof of Work ถูกต้อง (hash ขึ้นต้นด้วย '0000')

### 4️⃣ **Genesis Block**
บล็อกแรกสุดใน chain ที่ถูกสร้างอัตโนมัติเมื่อเริ่มต้น Blockchain โดยมี:
- `index = 0`
- `previous_hash = '0'`
- `body = 'Genesis Block'`

---

## 🚀 วิธีการรัน

### 1. **รันแบบ Local (ทดสอบพื้นฐาน)**
- `previous_hash = '0'`
- `body = 'Genesis Block'`

## วิธีการรัน

### 1. **รันแบบ Local (ทดสอบพื้นฐาน)**

ทดสอบการสร้างบล็อกและตรวจสอบ chain แบบ command line:

```bash
python -m cmd.local.main
```

**ผลลัพธ์ที่ได้:**

✅ การขุดบล็อกใหม่พร้อม nonce ที่พบ  
✅ Previous hash และ current hash ของแต่ละบล็อก  
✅ สถานะความถูกต้องของ chain

### 2. **รันแบบ Web API Server (Flask)**

เริ่มต้น REST API server สำหรับจัดการ blockchain ผ่าน HTTP:

```bash
python -m cmd.serve.app
```

Server จะรันที่: **`http://127.0.0.1:5000`**

---

## 📡 API Endpoints

### 📥 **เพิ่มข้อมูลรอการขุด**.0.0.1:5000`

### API Endpoints

#### **เพิ่มข้อมูลรอการขุด**
```http
POST /add_data
Content-Type: application/json
  "amount": 5
}
```

### ⛏️ **ขุดบล็อกใหม่**
}
```

GET /mine_block
```

> จะนำข้อมูลทั้งหมดที่รออยู่มาสร้างเป็นบล็อกใหม่

### 🔍 **ดูข้อมูล Blockchain ทั้งหมด**นบล็อกใหม่
GET /get_chain
```

### ✔️ **ตรวจสอบความถูกต้องของ Chain**
```

#### **ตรวจสอบความถูกต้องของ Chain**
GET /is_valid
```

### 🔴 **จำลองการโจมตี (Hack)**
#### **จำลองการโจมตี (Hack)**
```http
POST /hack_block
Content-Type: application/json

{
  "data": "fake transaction"
}
```

### 👋 **ทดสอบ Server**
#### **ทดสอบ Server**
GET /hello
```

---

## 💡 ตัวอย่างการใช้งาน

1. **เริ่ม server:**
   ```bash
   python -m cmd.serve.app
   ```
## 🛠️ เทคโนโลยีที่ใช้

| เทคโนโลยี | คำอธิบาย |
|-----------|----------|
| **Python 3.x** | ภาษาหลักในการพัฒนา |
| **Flask** | Web framework สำหรับ REST API |
| **hashlib** | สำหรับการเข้ารหัส SHA-256 |
| **JSON** | รูปแบบข้อมูลในการแลกเปลี่ยนข้อมูล |

---

## 📝 หมายเหตุ

- Genesis Block จะถูกสร้างอัตโนมัติเมื่อเริ่มต้น Blockchain
- การขุดแต่ละบล็อกจะใช้เวลาขึ้นอยู่กับความยาก (จำนวน 0 นำหน้า hash)
- สามารถทดสอบ API ด้วย Postman หรือ curl ได้n

4. **ดู chain:**  
   GET `/get_chain` เพื่อดูบล็อกทั้งหมด

5. **ตรวจสอบ:**  
   GET `/is_valid` เพื่อตรวจสอบความถูกต้อง

---

## 🛠️ เทคโนโลยีที่ใช้
## เทคโนโลยีที่ใช้

- **Python 3.x**: ภาษาหลักในการพัฒนา
- **Flask**: Web framework สำหรับ REST API
- **hashlib**: สำหรับการเข้ารหัส SHA-256
- **JSON**: รูปแบบข้อมูลในการแลกเปลี่ยนข้อมูล