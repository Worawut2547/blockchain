# 🔗 Blockchain Project

โปรเจกต์ระบบ Blockchain แบบกระจายศูนย์ (Decentralized) ที่พัฒนาด้วย Python รองรับการสร้างบล็อก การขุด (Mining) ด้วย Proof of Work การตรวจสอบความถูกต้อง และระบบ Consensus สำหรับการกู้คืนข้อมูลอัตโนมัติ

---

## 📁 โครงสร้างโปรเจกต์

```
blockchain/
├── core/                      # โมดูลหลักของระบบ Blockchain
│   ├── __init__.py
│   └── blockchain.py          # Class Block, Blockchain, Mining, NodeManager
├── cmd/
│   ├── local/                 # โปรแกรมทดสอบแบบ Local
│   │   ├── __init__.py
│   │   └── main.py            # ทดสอบ blockchain พื้นฐาน
│   └── serve/                 # Web API Server
│       ├── __init__.py
│       └── app.py             # Flask REST API Server
├── nodes.json                 # ไฟล์เก็บรายชื่อ node ในเครือข่าย
└── README.md
```

---

## 🧩 โครงสร้างและการทำงานของแต่ละส่วน

### 1️⃣ **Class Block** (`core/blockchain.py`)

เป็น Class ที่ใช้สร้างบล็อกแต่ละบล็อกใน Blockchain

#### **Attributes (คุณสมบัติ)**
- `index`: ลำดับของบล็อกใน chain (เริ่มจาก 0)
- `timestamp`: เวลาที่สร้างบล็อก
- `body`: ข้อมูลที่เก็บในบล็อก (ธุรกรรม หรือข้อมูลใดๆ)
- `previous_hash`: Hash ของบล็อกก่อนหน้า (เชื่อมโยงบล็อกเข้าด้วยกัน)
- `nonce`: ตัวเลขที่ได้จากการขุด (Proof of Work)
- `current_hash`: Hash ของบล็อกปัจจุบัน (คำนวณจากข้อมูลทั้งหมด)

#### **Methods (เมธอด)**
```python
hash()
```
- **ทำอะไร**: คำนวณ SHA-256 hash จากข้อมูลทั้งหมดในบล็อก
- **กระบวนการ**:
  1. รวมข้อมูล (index, timestamp, body, previous_hash, nonce) เป็น dictionary
  2. แปลงเป็น JSON string และ encode เป็น bytes
  3. ใช้ SHA-256 เข้ารหัสและคืนค่าเป็น hexadecimal string

```python
to_dict()
```
- **ทำอะไร**: แปลง Block object เป็น dictionary เพื่อส่งผ่าน API
- **ใช้เมื่อ**: ต้องการส่งข้อมูลบล็อกเป็น JSON response

---

### 2️⃣ **Class Blockchain** (`core/blockchain.py`)

เป็น Class หลักที่จัดการ chain ของบล็อก

#### **Attributes (คุณสมบัติ)**
- `chain`: List ที่เก็บบล็อกทั้งหมดตามลำดับ
- `pending_data`: List เก็บข้อมูลที่รอการขุด
- `pending_hack`: Dictionary เก็บข้อมูลการโจมตีที่รอดำเนินการ
- `nodes`: Set เก็บ URL ของ node อื่นๆ ในเครือข่าย
- `network`: Object ของ NodeManager สำหรับจัดการ node

#### **Methods สำคัญ**

##### 📌 **create_genesis_block()**
```python
def create_genesis_block(self)
```
- **ทำอะไร**: สร้างบล็อกแรกสุด (Genesis Block) ของ chain
- **เมื่อไหร่**: เรียกอัตโนมัติตอน `__init__` Blockchain
- **คุณสมบัติ**:
  - `index = 0`
  - `previous_hash = '0'`
  - `body = 'Genesis Block'`

##### 📌 **create_block(body)**
```python
def create_block(self, body)
```
- **ทำอะไร**: สร้างบล็อกใหม่และเพิ่มลงใน chain
- **กระบวนการ**:
  1. ดึงบล็อกล่าสุด (previous_block)
  2. เริ่มขุด (Mining) เพื่อหา nonce ใหม่
  3. สร้าง Block ใหม่ด้วย index, timestamp, body, previous_hash, nonce
  4. เพิ่มบล็อกใหม่เข้า chain
- **คืนค่า**: Block object ที่สร้างใหม่

##### 📌 **is_chain_valid()**
```python
def is_chain_valid(self)
```
- **ทำอะไร**: ตรวจสอบความถูกต้องของ chain ทั้งหมด
- **ตรวจสอบ 3 ข้อ**:
  1. **Hash ถูกต้อง**: `current_hash` ตรงกับการคำนวณ `hash()` ใหม่
  2. **การเชื่อมโยง**: `previous_hash` ตรงกับ `current_hash` ของบล็อกก่อนหน้า
  3. **Proof of Work**: nonce ที่ใช้ทำให้ hash ขึ้นต้นด้วย '0000'
- **คืนค่า**: `True` ถ้า valid, `False` ถ้าถูกแก้ไข

##### 📌 **add_data(data)**
```python
def add_data(self, data)
```
- **ทำอะไร**: เพิ่มข้อมูลใหม่เข้า pending_data รอการขุด
- **คืนค่า**: Index ของบล็อกถัดไปที่จะถูกสร้าง

##### 📌 **resolve_conflicts(current_node)**
```python
def resolve_conflicts(self, current_node=None)
```
- **ทำอะไร**: ระบบ Consensus - เปรียบเทียบ chain กับ node อื่นในเครือข่าย
- **กระบวนการ**:
  1. โหลดรายชื่อ node จากไฟล์
  2. ตรวจสอบว่า chain ของตัวเองถูกต้องหรือไม่
  3. วนลูปขอ chain จาก node อื่นๆ ผ่าน `/raw_chain`
  4. เลือก chain ที่ยาวกว่าและถูกต้อง หรือ chain ที่ช่วยกู้คืนตัวเองจากการโจมตี
  5. แทนที่ chain ของตัวเองถ้าเจอ chain ที่ดีกว่า
- **คืนค่า**: `True` ถ้าแทนที่ chain, `False` ถ้าไม่แทนที่

##### 📌 **execute_pending_hack()**
```python
def execute_pending_hack(self)
```
- **ทำอะไร**: ดำเนินการโจมตีบล็อกที่รออยู่ใน pending_hack
- **กระบวนการ**:
  1. ดึงข้อมูล target_index และ fake_data
  2. แก้ไข body ของบล็อกเป้าหมาย
  3. Re-mine เพื่อหา nonce ใหม่ที่ทำให้ hash ถูกต้อง
  4. อัปเดต nonce และ current_hash
  5. ล้าง pending_hack
- **คืนค่า**: Dictionary ที่มีรายละเอียดการโจมตี

##### 📌 **check_chain_validity(chain)**
```python
def check_chain_validity(self, chain)
```
- **ทำอะไร**: ตรวจสอบ chain ที่เป็น list ของ Block objects
- **ใช้เมื่อ**: ตรวจสอบ chain ที่ได้จาก node อื่น

##### 📌 **is_chain_valid_json_chain(chain)**
```python
def is_chain_valid_json_chain(self, chain)
```
- **ทำอะไร**: ตรวจสอบ chain ที่เป็น JSON format (list of dict)
- **กระบวนการ**: แปลง JSON เป็น Block objects แล้วเรียก check_chain_validity

##### 📌 **convert_json_to_chain(chain)**
```python
def convert_json_to_chain(self, chain)
```
- **ทำอะไร**: แปลง JSON chain เป็น list ของ Block objects
- **ใช้เมื่อ**: รับ chain จาก API ของ node อื่น

---

### 3️⃣ **Class Mining** (`core/blockchain.py`)

เป็น Class สำหรับจัดการกระบวนการขุด (Mining)

#### **Method**
```python
@staticmethod
proof_of_work(previous_nonce)
```
- **ทำอะไร**: หา nonce ใหม่ที่ทำให้ hash ตรงตามเงื่อนไข
- **อัลกอริทึม**:
  ```
  equation = (new_nonce² - previous_nonce²)
  hash = SHA256(equation)
  หา nonce ที่ทำให้ hash ขึ้นต้นด้วย '0000'
  ```
- **กระบวนการ**:
  1. เริ่มจาก `new_nonce = 1`
  2. คำนวณ hash จากสมการ
  3. ถ้า hash ขึ้นต้นด้วย '0000' → return nonce
  4. ถ้าไม่ → เพิ่ม nonce และลองใหม่
- **คืนค่า**: nonce ที่ถูกต้อง

> **หมายเหตุ**: ยิ่งจำนวน '0' มาก ความยากในการขุดก็ยิ่งสูงขึ้น

---

### 4️⃣ **Class NodeManager** (`core/blockchain.py`)

เป็น Class สำหรับจัดการ node ในเครือข่าย Blockchain

#### **Attributes**
- `nodes`: Set เก็บ URL ของ node ทั้งหมด

#### **Methods**

##### 📌 **register_node(address)**
```python
def register_node(self, address)
```
- **ทำอะไร**: เพิ่ม node ใหม่เข้าเครือข่าย
- **กระบวนการ**:
  1. โหลดข้อมูล node จากไฟล์
  2. เพิ่ม address ใหม่เข้า set
  3. บันทึกลงไฟล์ `nodes.json`

##### 📌 **save_nodes_to_file()**
```python
def save_nodes_to_file(self)
```
- **ทำอะไร**: บันทึกรายชื่อ node ทั้งหมดลงไฟล์ `nodes.json`
- **Format**: JSON array ของ URL strings

##### 📌 **load_nodes_from_file()**
```python
def load_nodes_from_file(self)
```
- **ทำอะไร**: โหลดรายชื่อ node จากไฟล์ `nodes.json`
- **เมื่อไหร่**: เรียกก่อนทุกครั้งที่จะใช้ข้อมูล node

---

### 5️⃣ **Flask API Server** (`cmd/serve/app.py`)

เป็น REST API Server สำหรับจัดการ Blockchain ผ่าน HTTP

#### **API Endpoints**

##### 📥 **POST /add_data**
```json
{
  "sender": "Alice",
  "receiver": "Bob",
  "amount": 5
}
```
- **ทำอะไร**: เพิ่มข้อมูลเข้า pending_data รอการขุด
- **คืนค่า**: Index ของบล็อกที่จะถูกสร้าง

##### ⛏️ **GET /mine_block**
- **ทำอะไร**: ขุดบล็อกใหม่จากข้อมูลที่รออยู่
- **กระบวนการ**:
  1. เรียก `resolve_conflicts()` เพื่อ sync ก่อน
  2. ตรวจสอบความถูกต้องของ chain
  3. ถ้ามี `pending_hack` → execute_pending_hack()
  4. ถ้าไม่มี → create_block() ตามปกติ
  5. ล้าง pending_data
- **คืนค่า**: ข้อมูลบล็อกที่สร้างใหม่

##### 🔍 **GET /get_chain**
- **ทำอะไร**: ดึงข้อมูล chain ทั้งหมด
- **กระบวนการ**:
  1. เรียก `resolve_conflicts()` เพื่อ sync กับ node อื่น
  2. แปลง Block objects เป็น dictionary
  3. ส่งกลับพร้อมข้อมูลว่ามีการ replace chain หรือไม่
- **คืนค่า**: chain ทั้งหมด, ความยาว, และสถานะ

##### ✔️ **GET /is_valid**
- **ทำอะไร**: ตรวจสอบและกู้คืน chain อัตโนมัติ
- **กระบวนการ**:
  1. ตรวจสอบด้วย `is_chain_valid()`
  2. ถ้า invalid → เรียก `resolve_conflicts()` เพื่อกู้คืน
  3. ถ้ากู้คืนสำเร็จ → แจ้งว่า restored
  4. ถ้ากู้คืนไม่สำเร็จ → แจ้งว่า corrupted
- **คืนค่า**: สถานะความถูกต้องและการกู้คืน

##### 🔴 **POST /hack_block/\<index\>**
```json
{
  "body": "fake transaction data"
}
```
- **ทำอะไร**: จำลองการโจมตีบล็อก
- **กระบวนการ**:
  1. รับ index และข้อมูลปลอม
  2. เก็บใน `pending_hack`
  3. รอให้เรียก `/mine_block` เพื่อ execute
- **คืนค่า**: ข้อความแจ้งว่าต้อง mine เพื่อทำการโจมตี

##### 📡 **POST /node/register**
- **ทำอะไร**: ลงทะเบียน node ปัจจุบันเข้าเครือข่าย
- **กระบวนการ**:
  1. ดึง URL ของ node ปัจจุบัน
  2. เรียก `nodes.register_node()`
  3. บันทึกลงไฟล์
- **คืนค่า**: รายชื่อ node ทั้งหมด

##### 📤 **GET /raw_chain**
- **ทำอะไร**: ส่งข้อมูล chain แบบไม่ sync (สำหรับ node อื่นมาตรวจสอบ)
- **ใช้โดย**: endpoint `/is_valid` และ `resolve_conflicts()` ของ node อื่น
- **คืนค่า**: chain ทั้งหมดและความยาว

##### 👋 **GET /hello**
- **ทำอะไร**: ทดสอบว่า server ทำงาน
- **คืนค่า**: "Hello, Blockchain!"

---

### 6️⃣ **Local Testing** (`cmd/local/main.py`)

โปรแกรมทดสอบ Blockchain แบบง่าย

#### **การทำงาน**
1. สร้าง Blockchain instance
2. สร้างบล็อกที่ 1 ด้วยธุรกรรม "Alice pays Bob 5 BTC"
3. แสดง previous_hash และ current_hash
4. สร้างบล็อกที่ 2 ด้วยธุรกรรม "Bob pays Charlie 2 BTC"
5. แสดง previous_hash และ current_hash
6. ตรวจสอบความถูกต้องของ chain

#### **Output ตัวอย่าง**
```
----- Mining Block 1 -----
Mining new block...
New nonce found: 12345
previous hash: <genesis_hash>
current hash: <block1_hash>

----- Mining Block 2 -----
Mining new block...
New nonce found: 67890
previous hash: <block1_hash>
current hash: <block2_hash>

Chain valid? Valid
```

---

## 🚀 วิธีการรัน

### **1. รันแบบ Local (ทดสอบพื้นฐาน)**

ทดสอบการสร้างบล็อกและตรวจสอบ chain แบบ command line:

```bash
python -m cmd.local.main
```

### **2. รันแบบ Web API Server (Multi-Node)**

เริ่ม server หลายตัวเพื่อทดสอบระบบกระจายศูนย์:

#### **Node 1 (Port 5000)**
```bash
python -m cmd.serve.app -p 5000
```

#### **Node 2 (Port 5001)**
```bash
python -m cmd.serve.app -p 5001
```

#### **Node 3 (Port 5002)**
```bash
python -m cmd.serve.app -p 5002
```

---

## 💡 ตัวอย่างการใช้งาน

### **สถานการณ์ที่ 1: การทำงานปกติ**

1. **เริ่ม Node 1:**
   ```bash
   python -m cmd.serve.app -p 5000
   ```

2. **ลงทะเบียน Node:**
   ```bash
   POST http://localhost:5000/node/register
   ```

3. **เพิ่มข้อมูล:**
   ```bash
   POST http://localhost:5000/add_data
   Body: {"sender": "Alice", "receiver": "Bob", "amount": 10}
   ```

4. **ขุดบล็อก:**
   ```bash
   GET http://localhost:5000/mine_block
   ```

5. **ดู Chain:**
   ```bash
   GET http://localhost:5000/get_chain
   ```

### **สถานการณ์ที่ 2: การโจมตีและกู้คืน**

1. **โจมตีบล็อก 1:**
   ```bash
   POST http://localhost:5000/hack_block/1
   Body: {"body": "Alice pays Bob 9999 BTC"}
   ```

2. **Mine เพื่อ Execute Attack:**
   ```bash
   GET http://localhost:5000/mine_block
   ```

3. **ตรวจสอบความถูกต้อง (Node 2 จะตรวจพบ):**
   ```bash
   GET http://localhost:5001/is_valid
   ```
   → Node 2 จะเรียก `resolve_conflicts()` และกู้คืนจาก Node อื่นที่ถูกต้อง

### **สถานการณ์ที่ 3: Consensus Algorithm**

1. **Node 1 มี 3 blocks**
2. **Node 2 มี 5 blocks (ยาวกว่า)**
3. **Node 1 เรียก:**
   ```bash
   GET http://localhost:5000/get_chain
   ```
   → Node 1 จะได้รับ chain จาก Node 2 เพราะยาวกว่าและถูกต้อง

---

## 🛠️ เทคโนโลยีที่ใช้

| เทคโนโลยี | คำอธิบาย |
|-----------|----------|
| **Python 3.x** | ภาษาหลักในการพัฒนา |
| **Flask** | Web framework สำหรับ REST API |
| **hashlib** | สำหรับการเข้ารหัส SHA-256 |
| **JSON** | รูปแบบข้อมูลในการแลกเปลี่ยนข้อมูล |
| **requests** | HTTP client สำหรับสื่อสารระหว่าง node |
| **argparse** | จัดการ command line arguments |

---

## 📊 Flow Chart การทำงาน

### **Mining Process**
```
1. ได้รับคำสั่ง mine_block
2. resolve_conflicts() - sync กับ node อื่น
3. ตรวจสอบความถูกต้องของ chain
4. ถ้ามี pending_hack → execute_pending_hack()
   ถ้าไม่มี → create_block() ปกติ
5. Mining.proof_of_work() - หา nonce
6. สร้าง Block ใหม่
7. เพิ่มเข้า chain
8. ล้าง pending_data
```

### **Consensus Process**
```
1. Node ตรวจพบ chain ไม่ถูกต้อง
2. resolve_conflicts()
3. ดึง chain จาก node อื่นทั้งหมด
4. เปรียบเทียบความยาวและความถูกต้อง
5. เลือก chain ที่:
   - ยาวกว่า และถูกต้อง
   - หรือ ช่วยกู้คืน (healing)
6. แทนที่ chain ของตัวเอง
7. ล้าง pending_data
```

---

## 🔐 คุณสมบัติด้านความปลอดภัย

### **1. Immutability (ความไม่สามารถแก้ไขได้)**
- แต่ละบล็อกมี hash ที่คำนวณจากข้อมูลทั้งหมด
- การแก้ไขข้อมูลจะทำให้ hash เปลี่ยน
- บล็อกถัดไปจะตรวจพบเพราะ previous_hash ไม่ตรงกัน

### **2. Proof of Work**
- ป้องกันการสร้างบล็อกปลอมง่ายๆ
- ผู้โจมตีต้องใช้พลังคำนวณมากในการ re-mine

### **3. Consensus Mechanism**
- ถ้า chain ถูกโจมตี node อื่นจะช่วยกู้คืน
- เลือก chain ที่ยาวที่สุดและถูกต้อง (Longest Chain Rule)
- กระจายศูนย์ - ไม่มี single point of failure

### **4. Distributed Network**
- มี node หลายตัวเก็บสำเนา chain
- ถ้า node หนึ่งถูกโจมตี node อื่นยังคงมีข้อมูลถูกต้อง
- Self-healing ผ่าน resolve_conflicts()

---

## 📝 หมายเหตุสำคัญ

### **การทำงานของระบบ Consensus**
- ทุกครั้งที่เรียก `/get_chain` จะ sync กับ node อื่นอัตโนมัติ
- ถ้า chain ของตัวเองสั้นกว่า หรือไม่ถูกต้อง จะถูกแทนที่
- ต้องมี node อื่นในเครือข่ายที่มี chain ถูกต้องเพื่อกู้คืน

### **การโจมตี (Hack)**
- ใช้ `/hack_block` เพื่อจำลองการโจมตี
- ต้องเรียก `/mine_block` เพื่อ execute การโจมตี
- Node อื่นจะตรวจพบและสามารถกู้คืนได้

### **ไฟล์ nodes.json**
- เก็บรายชื่อ node ทั้งหมดในเครือข่าย
- ถูกโหลดและบันทึกอัตโนมัติ
- Format: `["http://localhost:5000", "http://localhost:5001"]`

### **การทดสอบ Multi-Node**
- ต้องรัน server หลายตัวบน port ต่างกัน
- ใช้ `/node/register` เพื่อเชื่อม node เข้าด้วยกัน
- สามารถทดสอบด้วย Postman, curl, หรือ Python requests

---

## 🎯 Use Cases

### **1. Cryptocurrency**
- เก็บธุรกรรมการโอนเงิน
- ป้องกันการใช้เงินซ้ำ (Double Spending)

### **2. Supply Chain**
- ติดตามสินค้าตลอดห่วงโซ่การผลิต
- ตรวจสอบความถูกต้องของข้อมูล

### **3. Medical Records**
- เก็บประวัติการรักษาที่ไม่สามารถแก้ไขได้
- กระจายข้อมูลอย่างปลอดภัย

### **4. Voting System**
- บันทึกคะแนนเสียงที่ไม่สามารถปลอมแปลงได้
- โปร่งใสและตรวจสอบได้

---

## 🚦 การขยายต่อยอด

- ✅ เพิ่มระบบ Digital Signature
- ✅ ปรับปรุง Consensus Algorithm (PoS, DPoS)
- ✅ เพิ่ม Smart Contract
- ✅ ปรับปรุง UI/Dashboard
- ✅ เพิ่มระบบ Merkle Tree
- ✅ Database Integration

---

**พัฒนาโดย**: Blockchain Development Team  
**เวอร์ชัน**: 1.0.0  
**อัปเดตล่าสุด**: December 2025
